function convertToCelsius(fahren) {}

function createMessage(fahren, celc) {}

function rand(limit) {}

// I have no idea what im even supposed to do, this makes no sense to me //

// -------------------- DONT NOT CHANGE THE CODE BELOW ---------------------- //

let fahren = prompt(
    "enter a number, we will convert that number from fahrenheit to celcius"
);
let celc = convertToCelcius(fahren);
let ooutput = createMessage(fahren, celc);
console.log(output);

fahren = prompt(
    "lets try that again. Enter a number, we will convert that number from fahrenheit to celsius"
);
celc = convertToCelsius(fahren);
output = createMessage(fahren, celc);
console.log(output);

fahren = rand(110);
celc = convertToCelsius(fahren);
output = createMessage(fahren, celc);
console.log(output);

fahren = rand(110);
celc = convertToCelsius(fahren);
output = createMessage(fahren, celc);
console.log(output);